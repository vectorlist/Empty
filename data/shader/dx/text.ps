Texture2D tex;
SamplerState samplerState;

struct PSInput
{
    float4 pos : SV_POSITION;
    float2 st : ST;
};

float4 PS(PSInput input) : SV_TARGET
{
    float4 color = float4(1, 1, 1, 0);
    color.a = tex.Sample(samplerState, input.st).r;
    return color;
}